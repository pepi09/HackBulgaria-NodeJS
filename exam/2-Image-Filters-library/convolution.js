"use strict";
var Q = require("q");

function monochrome(imageData, kernel){

  function edgeDetection(imageData){
    var defer = Q.defer();


  }



}
var xMarksTheSpot = [
[255,   0,   0,   0, 255],
[  0, 255,   0, 255,   0],
[  0,   0, 255,   0,   0]
[  0, 255,   0, 255,   0],
[255,   0,   0,   0, 255],
]


function getConvolutionMatrix(imageData, row, column, width, height){
  // widht and height are the dimensions of the kernel we want to apply\
  // row and column are the indexes of the element that we want
  // in the middle of the convolution matrix




}
